name: detect-spam-votes-cron

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

concurrency:
  group: detect-spam-votes-cron
  cancel-in-progress: false

jobs:
  ping:
    runs-on: ubuntu-latest
    timeout-minutes: 3
    steps:
      - name: Hit Vercel cron endpoint
        env:
          CRON_TARGET_URL: ${{ secrets.DETECT_SPAM_VOTES_TARGET_URL }}
          CRON_SECRET: ${{ secrets.CRON_SECRET }}
        run: |
          if [ -z "$CRON_TARGET_URL" ] || [ -z "$CRON_SECRET" ]; then
            echo "::error::DETECT_SPAM_VOTES_TARGET_URL or CRON_SECRET not set"
            exit 1
          fi
          http_code=$(curl -sS -o /tmp/resp.json -w "%{http_code}" \
            --max-time 120 \
            -X POST \
            -H "Authorization: Bearer $CRON_SECRET" \
            "$CRON_TARGET_URL")
          echo "HTTP $http_code"
          cat /tmp/resp.json || true
          echo ""
          if [ "$http_code" != "200" ]; then
            exit 1
          fi
